# Build

```bash
deno run --allow-read --allow-write main.tsx
```

# Development

```bash
deno install --allow-net --allow-read https://deno.land/std@0.141.0/http/file_server.ts
```

```bash
deno run --allow-read --allow-write --watch main.tsx
```
